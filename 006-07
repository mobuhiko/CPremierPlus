#include <stdio.h>
#include <string.h>

int main(void) {
	char word[100];
	printf("enter a word\n");
	scanf("%s", word);

	int length = strlen(word);
	
	int i = length - 1; // word[length] = '\0'
	for (i; i >= 0; i--) {
		printf("%c", word[i]);
	}

	return 0;
}
