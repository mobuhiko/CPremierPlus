#include <stdio.h>
#include <float.h>

int main(void) {
	double a = 1.0 / 3.0;
	float b = 1.0 / 3.0;
	printf("double: %.4f %.12f %.16f\n", a, a, a);
	printf("float: %.4f %.12f %.16f\n", a, a, a);
	printf("FLT_DIG = %f\n", FLT_DIG);
	printf("DBL_DIG = %f\n", DBL_DIG);
	return 0;
}
